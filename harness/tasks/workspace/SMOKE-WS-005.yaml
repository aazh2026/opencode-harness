id: SMOKE-WS-005
title: Workspace config set modifies configuration
category: smoke
fixture_project: fixtures/projects/cli-basic
description: Verify that opencode workspace config set modifies workspace configuration
expected_outcome: Workspace config set exits with code 0 and modifies config
preconditions:
  - opencode binary exists
entry_mode: Workspace
agent_mode: OneShot
provider_mode: Both
input:
  command: opencode
  args:
    - workspace
    - config
    - set
    - key
    - value
  cwd: /project
expected_assertions:
  - type: exit_code_equals
    value: 0
allowed_variance:
  exit_code:
    - 0
severity: Medium
tags:
  - smoke
  - workspace
  - config
execution_policy: ManualCheck
timeout_seconds: 30
on_missing_dependency: Warn
